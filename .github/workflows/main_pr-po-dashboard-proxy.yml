name: Legacy pr-po-dashboard-proxy deployment disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy deployment is disabled. Use the ssg-prpo-proxy workflow."
